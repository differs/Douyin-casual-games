{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:minigame-shell": "npm run build && node ./scripts/build-minigame-shell.mjs",
    "build:minigame-native": "node ./scripts/build-native-minigame.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
